const { species, hours } = require('../data/zoo_data');
const data = require('../data/zoo_data');

function getSchedule(scheduleTarget) {
  // Sem parametro, retorna Horario de funcionamento e animais do dia
  // parâmetros não seja um animal e dia, retorna objeto com os horários do dia e os animais em exibição'
  // ' com "Monday" , informar que o zoológico está fechado'
  // com animal , array com os dias q ele ta disponivel
  // com dia, retorna horario de funcionamento e os animais do dia
}
module.exports = getSchedule;
